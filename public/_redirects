# Affiliate redirects — rule 12.
#
# Every affiliate link on this site points at /go/<slug>, never at the
# destination. This file is what turns that into a real redirect, served by
# Cloudflare's static asset handler at the edge: no Worker script, no
# JavaScript, no request logging, nothing stateful.
#
# MUST AGREE WITH src/lib/affiliates.ts, line for line. Same slugs, same URLs.
# scripts/check-deploy-config.mjs fails the build if they diverge — two files
# that have to say the same thing with no check between them is what shipped
# every page on this site as a 307 (D46).
#
# FORMAT:  /go/<slug>   <destination>   302
#
# 302, NOT 301. A permanent redirect is cached by browsers indefinitely, so the
# day a programme changes its destination — or ends — every visitor who ever
# clicked the old one keeps going there, and nothing we deploy can reach them.
# Affiliate destinations are the least permanent URLs on the internet.
#
# Crawlers are kept off these paths by `Disallow: /go/` in robots.txt, and the
# links themselves carry rel="sponsored nofollow" (rule 12), enforced against
# the built HTML by scripts/check-links.mjs.
#
# THERE ARE NO PARTNERS YET. That is deliberate: the convention exists before
# the first link does, so nothing has to be retrofitted across a grown site.
# Adding one means an entry here AND in src/lib/affiliates.ts.

# ── Migration redirects — sources that must no longer be built ───────────────
#
# Format:  /old-path   /new-path   301
#
# scripts/check-deploy-config.mjs asserts, for every line below: the source is
# ABSENT from dist/, the destination is PRESENT in it, and the status is 301.
# The first of those is the one that matters — Cloudflare serves a real asset in
# preference to a redirect rule, so a rule whose source is still built is
# silently dead and every other gate stays green.
#
# THE MECHANISM IS PROVEN. Verified live on 2026-09-07 against the deploy of
# #74, which carried a throwaway `/_redirect-probe -> /about 301`:
#
#   /_redirect-probe                    301 -> https://quickoper.com/about
#   /_redirect-probe (following)        200 after 1 hop
#   /_redirect-probe-not-a-rule         404          <- the control
#   /about, /credit-card-interest       200          <- unaffected
#   /_redirect-probe?p=250000&r=5.5     301 -> /about?p=250000&r=5.5
#
# The control is the line that matters: an unlisted path 404s, so the 301 came
# from this file rather than from a Cloudflare dashboard rule answering ahead of
# the Worker (D60). The probe line has been removed; D81 carries the detail.
#
# The nine migration rules below were added in that change.

# ── The 2026-09 migration: nine root URLs moved under /finance ────────────────
#
# The root namespace is reserved for site-level pages; all vertical content
# lives under its vertical. These nine were finance content sitting at the root
# because finance was the only vertical when they shipped.
#
# 301, not 302: the move is permanent, and permanence is the entire claim being
# made to a crawler. Each source is asserted ABSENT from dist/ and each
# destination PRESENT in it by scripts/check-deploy-config.mjs — which is what
# proves the git mv actually happened rather than a copy.
#
# NEVER collapse these into `/* /finance/:splat 301`. It swallows /about,
# /apps, /methodology and every asset, and because its source is not a literal
# path no gate could catch it.
/15-year-vs-30-year-mortgage   /finance/15-year-vs-30-year-mortgage   301
/biweekly-mortgage-payments    /finance/biweekly-mortgage-payments    301
/coast-number                  /finance/coast-number                  301
/credit-card-interest          /finance/credit-card-interest          301
/minimum-payments              /finance/minimum-payments              301
/monthly-return-rate           /finance/monthly-return-rate           301
/mortgage-overpayment-timing   /finance/mortgage-overpayment-timing   301
/withdrawal-rate               /finance/withdrawal-rate               301
/verify                        /finance/verify                        301
